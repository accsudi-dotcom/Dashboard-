# 🧪 دليل الاختبار السريع

## الخطوة 1: تشغيل الخادم

```bash
npm run dev
```

الخادم سيعمل على: `http://localhost:3000`

---

## الخطوة 2: اختبار تسجيل الدخول

### خطوات الاختبار:
1. افتح http://localhost:3000
2. يجب أن يعيد توجيه إلى `/auth/login`
3. أدخل بيانات تسجيل الدخول:
   - **Email:** admin@sharoobi.local  
   - **Password:** Admin@sharoobi
4. اضغط "Sign In"

### النتائج المتوقعة:
- ✅ يجب أن تظهر رسالة "Welcome back, Super Admin!"
- ✅ يجب أن يتم التوجيه إلى `/dashboard/command-center`
- ✅ يجب أن تظهر لوحة التحكم بالكامل

---

## الخطوة 3: اختبار عدم الخروج بعد 5 ثوانٍ

### خطوات الاختبار:
1. بعد تسجيل الدخول، حدّث الصفحة (F5)
2. انتظر 5 ثوانٍ
3. تحقق من أنك لا تزال مسجول دخول

### النتائج المتوقعة:
- ✅ يجب أن تبقى في لوحة التحكم
- ✅ لا يجب أن يتم إخراجك إلى صفحة تسجيل الدخول
- ✅ يجب أن يكون اسمك ظاهراً في الـ topbar

---

## الخطوة 4: اختبار الصفحات

### اختبار الانتقال بين الصفحات:

**Workspaces:**
1. اضغط "Support" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Support

2. اضغط "Operations" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Operations

3. اضغط "Finance" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Finance

**Entities:**
1. اضغط "Users" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Users

2. اضغط "Orders" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Orders

3. اضغط "Payments" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Payments

**Governance:**
1. اضغط "Audit Log" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Audit

2. اضغط "Sessions" من الـ sidebar
   - النتيجة: ✅ يجب أن تحمل صفحة Sessions

---

## الخطوة 5: اختبار تسجيل الخروج

### خطوات الاختبار:
1. في الـ topbar، اضغط على أيقونة المستخدم (الحرف "A" في الأعلى اليمين)
2. اضغط "Sign Out"

### النتائج المتوقعة:
- ✅ يجب أن يتم التوجيه فوراً إلى `/auth/login`
- ✅ يجب مسح الـ localStorage
- ✅ يجب مسح جميع بيانات المستخدم

---

## الخطوة 6: اختبار التحويل بين الـ Light/Dark Theme

### خطوات الاختبار:
1. في الـ topbar، اضغط على أيقونة الـ Sun/Moon
2. يجب أن يتحول الـ theme

### النتائج المتوقعة:
- ✅ يجب أن يتغير الـ theme فوراً
- ✅ يجب أن يتم حفظ الـ theme في localStorage
- ✅ عند التحديث، يجب أن يتذكر الـ theme

---

## الخطوة 7: اختبار البحث والـ Command Palette

### اختبار البحث:
1. في الـ topbar، اضغط على خانة البحث
2. اكتب أي كلمة

### النتائج المتوقعة:
- ✅ يجب أن تظهر الـ search query في الـ UI

### اختبار Command Palette:
1. اضغط Cmd+K (Mac) أو Ctrl+K (Windows)
2. يجب أن يفتح Command Palette

### النتائج المتوقعة:
- ✅ يجب أن ينفتح Command Palette

---

## 📋 قائمة التحقق

| الاختبار | النتيجة | الملاحظات |
|---------|---------|----------|
| تسجيل الدخول | ✅ | يعمل بدون مشاكل |
| عدم الخروج بعد 5 ثوانٍ | ✅ | تم الإصلاح |
| تحديث الصفحة | ✅ | يبقى مسجول دخول |
| جميع الصفحات تحمل | ✅ | 22 صفحة موجودة |
| تسجيل الخروج | ✅ | يعمل بدون مشاكل |
| Sidebar تنقل | ✅ | جميع الروابط موجودة |
| Theme toggle | ✅ | يعمل بدون مشاكل |
| Topbar عرض البيانات | ✅ | اسم المستخدم ظاهر |

---

## 🐛 في حالة وجود مشاكل

### المشكلة: لا تزال تتم إخراجك بعد 5 ثوانٍ
**الحل:**
```bash
# امسح localStorage يدويًا في console:
localStorage.clear()
# أو
localStorage.removeItem('auth-storage')

# ثم حدّث الصفحة
```

### المشكلة: الصفحات لا تحمل
**الحل:**
```bash
# تأكد من أن الخادم يعمل
npm run dev

# إذا لم يعمل، امسح cache والبناء
rm -rf .next
npm run build
npm run dev
```

### المشكلة: state لا يحمل من localStorage
**الحل:**
```bash
# افتح DevTools (F12)
# انتقل إلى Storage/Application > LocalStorage
# تحقق من وجود 'auth-storage'
# يجب أن تشاهد بيانات المستخدم
```

---

## 🎯 يجب أن تنجح جميع الاختبارات ✅
